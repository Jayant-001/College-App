<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.srgplearningpoint_adminpanel">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SRGPLearningPointAdminPanel">
        <activity android:name=".DeleteNotice"></activity>
        <activity android:name=".UpdateTeacher" />
        <activity android:name=".UpdateFaculty" />
        <activity android:name=".AddTeacher" />
        <activity android:name=".UploadPDF" />
        <activity android:name=".UploadImage" />
        <activity android:name=".UploadNotice" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>